{
  "name": "roadsos-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":         "npm run dev --prefix my-app",
    "build":       "npm run build --prefix my-app",
    "start":       "npm run start --prefix my-app",
    "dev:backend": "npm run dev --prefix backend",
    "dev:all":     "concurrently \"npm run dev --prefix my-app\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
